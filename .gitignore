.env
.env.*
!.env.example
node_modules/
.next/
__pycache__/
*.pyc

# Unrelated project copied in for reference; has its own git history
xano-workspace/

# Filled PDFs generated for real cases at runtime (may contain real patient data)
app/public/generated/

# Local tooling
.claude/

# Runtime document cache (filled PDFs, per engine)
app/.doccache/
app/tsconfig.tsbuildinfo

# Runtime form-schema cache (per PDF hash)
app/.formcache/

# Runtime nearby-organizations cache (per category + location)
app/.nearbycache/
